export const setItemStorate = (key: string, value: string) => localStorage.setItem(key, value);

export const removeItemStorate = (key: string) => localStorage.removeItem(key);

export const getItemStorate = (key: string) => localStorage.getItem(key);
